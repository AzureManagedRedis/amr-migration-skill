{
  "$schema": "https://schema.management.azure.com/schemas/2019-04-01/deploymentTemplate.json#",
  "contentVersion": "1.0.0.0",
  "metadata": {
    "_migrationSource": "ACR Standard C2 (2.5 GB) → AMR Balanced_B3 (3 GB)",
    "_changes": [
      "Resource split: Microsoft.Cache/redis → redisEnterprise cluster + databases/default",
      "SKU: Standard/C/2 → Balanced_B3",
      "Eviction: allkeys-lru → AllKeysLRU (PascalCase)",
      "Clustering: Non-clustered (omitted — source non-clustered, target ≤ 24GB)",
      "Port: 6380 → 10000, clientProtocol: Encrypted",
      "Removed params: enableNonSslPort, redisVersion, aadEnabled, skuName, skuFamily, skuCapacity, clusteringPolicy",
      "Added params: amrSkuName, evictionPolicy, clientProtocol, port",
      "Preserved: name, location, tags, identity, minimumTlsVersion, publicNetworkAccess",
      "No Premium features in source — clean conversion, no persistence/VNet/zones/firewall"
    ]
  },
  "parameters": {
    "redisCacheName": {
      "type": "string",
      "metadata": { "description": "Name of the Azure Managed Redis instance." }
    },
    "location": {
      "type": "string",
      "defaultValue": "[resourceGroup().location]",
      "metadata": { "description": "Azure region." }
    },
    "amrSkuName": {
      "type": "string",
      "defaultValue": "Balanced_B3",
      "metadata": { "description": "AMR SKU name (compound format)." }
    },
    "minimumTlsVersion": {
      "type": "string",
      "defaultValue": "1.2",
      "metadata": { "description": "Minimum TLS version." }
    },
    "publicNetworkAccess": {
      "type": "string",
      "defaultValue": "Enabled",
      "allowedValues": ["Enabled", "Disabled"],
      "metadata": { "description": "Public network access." }
    },
    "evictionPolicy": {
      "type": "string",
      "defaultValue": "AllKeysLRU",
      "allowedValues": [
        "VolatileLRU", "AllKeysLRU", "VolatileLFU", "AllKeysLFU",
        "VolatileRandom", "AllKeysRandom", "VolatileTTL", "NoEviction"
      ],
      "metadata": { "description": "Eviction policy (PascalCase)." }
    },
    "clientProtocol": {
      "type": "string",
      "defaultValue": "Encrypted",
      "metadata": { "description": "Client protocol." }
    },
    "port": {
      "type": "int",
      "defaultValue": 10000,
      "metadata": { "description": "Redis port." }
    },
    "tags": {
      "type": "object",
      "defaultValue": {},
      "metadata": { "description": "Resource tags." }
    }
  },
  "resources": [
    {
      "type": "Microsoft.Cache/redisEnterprise",
      "apiVersion": "2025-07-01",
      "name": "[parameters('redisCacheName')]",
      "location": "[parameters('location')]",
      "tags": "[parameters('tags')]",
      "identity": {
        "type": "SystemAssigned"
      },
      "sku": {
        "name": "[parameters('amrSkuName')]"
      },
      "properties": {
        "minimumTlsVersion": "[parameters('minimumTlsVersion')]",
        "publicNetworkAccess": "[parameters('publicNetworkAccess')]"
      }
    },
    {
      "type": "Microsoft.Cache/redisEnterprise/databases",
      "apiVersion": "2025-07-01",
      "name": "[concat(parameters('redisCacheName'), '/default')]",
      "dependsOn": [
        "[resourceId('Microsoft.Cache/redisEnterprise', parameters('redisCacheName'))]"
      ],
      "properties": {
        "clientProtocol": "[parameters('clientProtocol')]",
        "port": "[parameters('port')]",
        "evictionPolicy": "[parameters('evictionPolicy')]",
        "modules": []
      }
    }
  ],
  "outputs": {
    "cacheId": {
      "type": "string",
      "value": "[resourceId('Microsoft.Cache/redisEnterprise', parameters('redisCacheName'))]"
    },
    "cacheHostName": {
      "type": "string",
      "value": "[concat(parameters('redisCacheName'), '.', parameters('location'), '.redis.azure.net')]"
    },
    "cachePort": {
      "type": "int",
      "value": "[parameters('port')]"
    }
  }
}
